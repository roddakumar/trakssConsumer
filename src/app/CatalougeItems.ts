
export interface CatalougeItem {
          catalogueItemId: string;
          catalogueTitle: string;
          brand: string;
          maximumRetailPrice: number;
          imageUrls: [];
          category: string;
          description: string;
          subCategory: string;
          numberOfUnits: number;
          resellingCategories: string;
  CatalogueId(arg0: string, CatalogueId: any, units: number, durationInMonths: any);
      }
